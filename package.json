{
  "name": "@rcompat/monorepo",
  "description": "Standard library for JavaScript runtimes",
  "type": "module",
  "bugs": "https://github.com/rcompat/rcompat/issues",
  "license": "MIT",
  "files": [],
  "repository": "https://github.com/rcompat/rcompat",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "publish": "pnpm -r --access=public publish",
    "upgrade": "pnpm -r update -i -L",
    "lint": "pnpm -r lint",
    "lint:fix": "eslint . --fix",
    "test": "npx proby",
    "sweep": "rm -rf node_modules pnpm-lock.yaml packages/*/node_modules"
  },
  "private": true,
  "devDependencies": {
    "@rcompat/test": "workspace:^",
    "@types/bun": "^1.3.14",
    "@types/deno": "^2.7.0",
    "@types/node": "^25.9.1",
    "@typescript/native-preview": "7.0.0-dev.20260601.1",
    "apekit": "^0.0.58",
    "eslint": "^10.4.1",
    "proby": "workspace:^"
  }
}
