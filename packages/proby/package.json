{
  "name": "proby",
  "version": "0.13.0",
  "description": "Standard library test runner",
  "bugs": "https://github.com/rcompat/rcompat/issues",
  "license": "MIT",
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "bin": "lib/bin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/rcompat/rcompat",
    "directory": "packages/proby"
  },
  "dependencies": {
    "@rcompat/assert": "workspace:^",
    "@rcompat/cli": "workspace:^",
    "@rcompat/env": "workspace:^",
    "@rcompat/fs": "workspace:^",
    "@rcompat/io": "workspace:^",
    "@rcompat/is": "workspace:^",
    "@rcompat/runtime": "workspace:^",
    "pema": "^0.5.0"
  },
  "peerDependencies": {
    "@rcompat/test": "workspace:^"
  },
  "peerDependenciesMeta": {
    "@rcompat/test": {
      "optional": true
    }
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./lib",
    "prepublishOnly": "npm run build",
    "test": "npx proby"
  },
  "type": "module",
  "imports": {
    "#*": {
      "source": "./src/*.ts",
      "default": "./lib/*.js"
    }
  },
  "exports": {
    "./config": {
      "source": "./src/config.ts",
      "default": "./lib/config.js"
    }
  }
}
