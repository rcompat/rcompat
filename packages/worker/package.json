{
  "name": "@rcompat/worker",
  "version": "0.16.0",
  "description": "Standard library workers",
  "bugs": "https://github.com/rcompat/rcompat/issues",
  "license": "MIT",
  "files": [
    "/lib/**/*.js",
    "/lib/**/*.d.ts",
    "!/**/*.spec.*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/rcompat/rcompat",
    "directory": "packages/worker"
  },
  "scripts": {
    "build": "npm run clean && tsgo",
    "test": "npx proby",
    "clean": "rm -rf ./lib",
    "lint": "eslint .",
    "prepublishOnly": "npm run build"
  },
  "type": "module",
  "exports": {
    ".": {
      "@rcompat/source": "./src/bun.ts",
      "bun": "./lib/bun.js",
      "deno": "./lib/deno.js",
      "node": "./lib/node.js"
    }
  }
}
